<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.iiseeuu.helper">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>

    <application
        android:name=".BaseApplication"
        android:allowBackup="true"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!--登录-->
        <activity android:name=".activity.LoginActivity"/>
        <!--主页-->
        <activity android:name=".activity.MainActivity"/>
        <!--折线图-->
        <activity android:name=".activity.TendencyActivity"/>
        <!--用户详情-->
        <activity android:name=".activity.UserDetailActivity"/>
        <!--打印机详情-->
        <activity android:name=".activity.PrintDetailActivity"/>
        <!--订单详情-->
        <activity android:name=".activity.OrdersDetailActivity"/>
        <!--授权用户-->
        <activity android:name=".activity.AccreditUserActivity"/>
        <!--图片预览-->
        <activity android:name=".activity.ImagePreviewActivity"/>
    </application>

</manifest>